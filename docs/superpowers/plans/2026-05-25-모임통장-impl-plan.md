# 모임통장(Wallet) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** eu-ssya-bot에 모임통장(공유 운영비 관리) 기능을 추가한다. Discord Administrator만 /모임통장 등록·입금·출금·관리 명령으로 운영하고, 잔액은 채널명에, 모든 거래는 채널 메시지로 자동 공개된다.

**Architecture:** `wallet_cog.py`(신규)에 Cog 클래스로 분리하고 `bot.py`는 `load_extension` 한 줄만 추가한다. 기존 `_data_lock`/`load_data`/`save_data`를 import해 같은 `rss_data.json`을 공유한다. 채널명 갱신은 `_pending_balance` 코얼레싱 dict + 5분 cooldown rename worker로 Discord rate-limit(2/10min)을 회피한다.

**Tech Stack:** Python 3.13, discord.py 2.7.1, stdlib(uuid, datetime, asyncio), existing python-dotenv. **No new pip dependencies.**

**Spec:** `docs/superpowers/specs/2026-05-25-모임통장-design.md` (user-approved)

**Verification model:** spec이 명시한 "pytest 풀스위트 out of scope" 결정에 맞춰 TDD는 `scripts/verify_wallet.py` 어설션 스크립트로, Discord-side는 수동 체크리스트로 대체.

**Environment notes:**
- Working dir: `D:\dev\eu-ssya-bot`
- Python: `venv\Scripts\python.exe` (Python 3.13)
- Shell: PowerShell on Windows
- All commands assume cwd is the repo root

---

## Task 1: Branch 생성 + wallet_cog.py 스켈레톤 + bot.py 통합

**Goal:** wallet 기능 작업용 브랜치 + 빈 Cog 등록까지. 기존 RSS 동작은 무변경.

**Files:**
- Create: `wallet_cog.py`
- Modify: `bot.py` (setup_hook 1줄 추가)

- [ ] **Step 1: feat/wallet 브랜치 생성**

```powershell
cd D:\dev\eu-ssya-bot
git checkout -b feat/wallet
git status
```
Expected: `On branch feat/wallet, working tree clean (or only previous modifications)`

- [ ] **Step 2: wallet_cog.py 스켈레톤 작성 (실패 검증용 최소 코드)**

Create `D:\dev\eu-ssya-bot\wallet_cog.py`:

```python
"""모임통장(Wallet) Cog — 스터디 커뮤니티 운영비 관리."""
from __future__ import annotations

import logging

import discord
from discord.ext import commands

logger = logging.getLogger("eu_ssya_bot")


class WalletCog(commands.Cog):
    """모임통장 명령 + UI + rename worker."""

    def __init__(self, bot: commands.Bot) -> None:
        self.bot = bot


async def setup(bot: commands.Bot) -> None:
    await bot.add_cog(WalletCog(bot))
```

- [ ] **Step 3: bot.py에서 wallet_cog 로드**

Edit `D:\dev\eu-ssya-bot\bot.py`. Find `setup_hook`:

```python
@bot.event
async def setup_hook():
    rss_loop.start()
```

Change to:

```python
@bot.event
async def setup_hook():
    await bot.load_extension("wallet_cog")
    rss_loop.start()
```

- [ ] **Step 4: py_compile + import 스모크 검증**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import ast; [ast.parse(open(f, encoding='utf-8').read()) for f in ('bot.py','wallet_cog.py')]; print('ast OK')"
Remove-Item Env:DISCORD_BOT_TOKEN -ErrorAction SilentlyContinue
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('import OK')"
```
Expected: 3개 명령 모두 exit 0, `ast OK` 및 `import OK` 출력.

- [ ] **Step 5: Commit**

```powershell
git add bot.py wallet_cog.py
git commit -m "feat(wallet): wallet_cog 스켈레톤 + bot.py에 load_extension 추가"
```

---

## Task 2: 모듈 상태 + 순수 유틸 함수

**Goal:** rename 디바운스 큐 + 통화/날짜/잔액 계산 유틸. 모두 Discord 무관 pure functions라 어설션 스크립트로 검증.

**Files:**
- Modify: `wallet_cog.py` (상단에 module-level 상태 + 유틸 함수 추가)
- Create: `scripts/verify_wallet.py` (어설션 스크립트)

- [ ] **Step 1: 실패하는 어설션 스크립트 작성**

Create directory `D:\dev\eu-ssya-bot\scripts\` if not exists. Create `D:\dev\eu-ssya-bot\scripts\verify_wallet.py`:

```python
"""모임통장 pure function 검증 — TOKEN 없이 실행 가능."""
import sys
from datetime import date

sys.path.insert(0, ".")
import wallet_cog as W

# format_krw
assert W.format_krw(50000) == "50,000원", W.format_krw(50000)
assert W.format_krw(-15000) == "-15,000원", W.format_krw(-15000)
assert W.format_krw(0) == "0원"
assert W.format_krw(1234567) == "1,234,567원"

# _format_channel_name
assert W._format_channel_name(285000) == "💰-285,000원"
assert W._format_channel_name(0) == "💰-0원"

# _parse_date 유효
assert W._parse_date("2026-11-28") == date(2026, 11, 28)
assert W._parse_date("2026-01-01") == date(2026, 1, 1)

# _parse_date 무효 — ValueError 발생해야
for bad in ["2026/11/28", "2026-13-32", "20261128", "yesterday", "", "2026-11", "2026-2-3"]:
    try:
        W._parse_date(bad)
        raise AssertionError(f"_parse_date('{bad}') should have raised")
    except ValueError:
        pass

# compute_new_balance
assert W.compute_new_balance(100000, "income", 50000) == 150000
assert W.compute_new_balance(100000, "expense", 30000) == 70000
assert W.compute_new_balance(0, "income", 1) == 1
try:
    W.compute_new_balance(0, "weird", 1)
    raise AssertionError("unknown kind should raise")
except ValueError:
    pass

# validate_amount
assert W.validate_amount(1) is None
assert W.validate_amount(50000) is None
assert W.validate_amount(0) is not None  # error message
assert W.validate_amount(-100) is not None

# validate_memo
assert W.validate_memo("") is None
assert W.validate_memo("a" * 200) is None
assert W.validate_memo("a" * 201) is not None

# 상수 확인
assert W.RENAME_COOLDOWN_SECONDS >= 300
assert W.RENAME_WORKER_INTERVAL_SECONDS > 0
assert isinstance(W._pending_balance, dict)
assert isinstance(W._last_rename, dict)

print("verify_wallet: all assertions PASS")
```

- [ ] **Step 2: 어설션 실패 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `AttributeError: module 'wallet_cog' has no attribute 'format_krw'` (또는 유사). Exit non-zero.

- [ ] **Step 3: wallet_cog.py에 상태 + 유틸 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Replace contents with:

```python
"""모임통장(Wallet) Cog — 스터디 커뮤니티 운영비 관리."""
from __future__ import annotations

import datetime
import logging
from typing import Dict, Optional

import discord
from discord.ext import commands

logger = logging.getLogger("eu_ssya_bot")

# ---------------- 모듈 상태 ----------------
# 채널 ID -> 적용 대기 중인 최신 잔액. rename 워커가 소비한다.
_pending_balance: Dict[int, int] = {}
# 채널 ID -> 마지막 rename 성공 시각 (asyncio loop monotonic seconds).
_last_rename: Dict[int, float] = {}

# Discord 채널명 rate-limit: 10분당 2회 → 5분 floor (안전 마진).
RENAME_COOLDOWN_SECONDS = 300
# rename 워커 주기.
RENAME_WORKER_INTERVAL_SECONDS = 60
# 기본 채널명 포맷 — `{잔액}` 위치 자동 치환.
DEFAULT_WALLET_NAME_PREFIX = "💰-"
# 메모 최대 길이.
MAX_MEMO_LEN = 200
# View timeout (초).
VIEW_TIMEOUT_SECONDS = 600
# KST 타임존.
_KST = datetime.timezone(datetime.timedelta(hours=9))


# ---------------- 순수 유틸 함수 (Discord 무관, 테스트 가능) ----------------
def format_krw(amount: int) -> str:
    """정수 원화를 천 단위 구분 + '원' 접미사로 포맷. 음수는 '-' 접두사."""
    return f"{amount:,}원"


def _format_channel_name(balance: int) -> str:
    """잔액을 채널명 포맷 '💰-285,000원' 형태로 변환."""
    return f"{DEFAULT_WALLET_NAME_PREFIX}{balance:,}원"


def _now_kst_iso() -> str:
    """현재 시각을 KST ISO-8601(초 단위)로 반환."""
    return datetime.datetime.now(tz=_KST).isoformat(timespec="seconds")


def _today_kst_iso() -> str:
    """오늘 날짜를 KST 기준 'YYYY-MM-DD'로 반환."""
    return datetime.datetime.now(tz=_KST).date().isoformat()


def _parse_date(s: str) -> datetime.date:
    """ISO YYYY-MM-DD 엄격 파싱. 유효하지 않으면 ValueError.

    `datetime.date.fromisoformat`이 '2026-2-3' 같은 비표준도 허용하므로,
    엄격하게 'YYYY-MM-DD' 정확히 10자 + 두 자리 month/day인지 확인한다.
    """
    if not isinstance(s, str) or len(s) != 10 or s[4] != "-" or s[7] != "-":
        raise ValueError(f"날짜 형식이 잘못되었습니다: {s!r} (YYYY-MM-DD 형식이어야 합니다)")
    return datetime.date.fromisoformat(s)


def compute_new_balance(old_balance: int, kind: str, amount: int) -> int:
    """잔액 적용. kind는 'income' 또는 'expense'. amount는 양수."""
    if kind == "income":
        return old_balance + amount
    if kind == "expense":
        return old_balance - amount
    raise ValueError(f"unknown kind: {kind!r}")


def validate_amount(amount: int) -> Optional[str]:
    """유효하면 None, 아니면 한국어 에러 메시지."""
    if not isinstance(amount, int) or amount <= 0:
        return "금액은 1원 이상의 정수여야 합니다."
    return None


def validate_memo(memo: str) -> Optional[str]:
    """유효하면 None, 아니면 한국어 에러 메시지."""
    if not isinstance(memo, str):
        return "메모는 문자열이어야 합니다."
    if len(memo) > MAX_MEMO_LEN:
        return f"메모는 {MAX_MEMO_LEN}자 이하로 입력해주세요."
    return None


# ---------------- Cog ----------------
class WalletCog(commands.Cog):
    """모임통장 명령 + UI + rename worker."""

    def __init__(self, bot: commands.Bot) -> None:
        self.bot = bot


async def setup(bot: commands.Bot) -> None:
    await bot.add_cog(WalletCog(bot))
```

- [ ] **Step 4: 어설션 통과 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `verify_wallet: all assertions PASS`, exit 0.

- [ ] **Step 5: py_compile + import 검증**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import wallet_cog; print(wallet_cog.format_krw(100000))"
```
Expected: `100,000원`

- [ ] **Step 6: Commit**

```powershell
git add wallet_cog.py scripts\verify_wallet.py
git commit -m "feat(wallet): 모듈 상태 + 순수 유틸 함수 (format_krw, _parse_date, compute_new_balance 등) + 어설션 스크립트"
```

---

## Task 3: bot.py의 load_data에 wallets={} 기본값 추가

**Goal:** `load_data()`가 `wallets` 키 누락 시 빈 dict로 초기화. 기존 RSS 동작 100% 유지.

**Files:**
- Modify: `bot.py` (load_data 함수)
- Create: `scripts/verify_load_data.py`

- [ ] **Step 1: 실패하는 어설션 스크립트 작성**

Create `D:\dev\eu-ssya-bot\scripts\verify_load_data.py`:

```python
"""bot.load_data가 wallets 키를 자동 초기화하는지 검증."""
import json
import os
import sys
import tempfile

sys.path.insert(0, ".")
import bot

# 새 임시 파일 (없는 파일)
tmp1 = os.path.join(tempfile.gettempdir(), "_test_load_data_missing.json")
if os.path.exists(tmp1):
    os.remove(tmp1)
bot.DATA_FILE = tmp1
d = bot.load_data()
assert d == {"feeds": [], "wallets": {}}, f"missing file: {d!r}"

# feeds만 있고 wallets 없는 파일
tmp2 = os.path.join(tempfile.gettempdir(), "_test_load_data_feeds_only.json")
with open(tmp2, "w", encoding="utf-8") as f:
    json.dump({"feeds": [{"url": "x", "channel_id": 1}]}, f)
bot.DATA_FILE = tmp2
d = bot.load_data()
assert d["feeds"][0]["url"] == "x"
assert d.get("wallets") == {}, f"wallets key missing: {d!r}"
os.remove(tmp2)

# wallets가 dict가 아닌 malformed 파일 — 빈 dict로 강제
tmp3 = os.path.join(tempfile.gettempdir(), "_test_load_data_malformed.json")
with open(tmp3, "w", encoding="utf-8") as f:
    json.dump({"feeds": [], "wallets": "not a dict"}, f)
bot.DATA_FILE = tmp3
d = bot.load_data()
assert d["wallets"] == {}, f"malformed wallets not normalized: {d!r}"
os.remove(tmp3)

# 정상 wallets 존재 — 그대로 유지
tmp4 = os.path.join(tempfile.gettempdir(), "_test_load_data_with_wallets.json")
with open(tmp4, "w", encoding="utf-8") as f:
    json.dump({"feeds": [], "wallets": {"42": {"balance": 100}}}, f)
bot.DATA_FILE = tmp4
d = bot.load_data()
assert d["wallets"]["42"]["balance"] == 100
os.remove(tmp4)

print("verify_load_data: all assertions PASS")
```

- [ ] **Step 2: 어설션 실패 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_load_data.py
```
Expected: `AssertionError` on first assertion — current `load_data` returns `{"feeds": []}` without `wallets`.

- [ ] **Step 3: bot.py의 load_data 수정**

Edit `D:\dev\eu-ssya-bot\bot.py`. Find the function `load_data` (currently around lines 36-55). Two changes:

**Change A** — 파일 없을 때 fallback:

Replace:
```python
    if not os.path.exists(DATA_FILE):
        return {"feeds": []}
```
With:
```python
    if not os.path.exists(DATA_FILE):
        return {"feeds": [], "wallets": {}}
```

**Change B** — 파싱 실패 fallback:

Replace:
```python
    except (json.JSONDecodeError, OSError):
        # 파일이 깨졌거나 비어있을 때
        return {"feeds": []}
```
With:
```python
    except (json.JSONDecodeError, OSError):
        # 파일이 깨졌거나 비어있을 때
        return {"feeds": [], "wallets": {}}
```

**Change C** — 정규화 블록 끝에 wallets 보장 추가:

Find the block:
```python
    # feeds 키가 없거나 list가 아니면 초기화
    if "feeds" not in data or not isinstance(data["feeds"], list):
        data["feeds"] = []

    return data
```
Change to:
```python
    # feeds 키가 없거나 list가 아니면 초기화
    if "feeds" not in data or not isinstance(data["feeds"], list):
        data["feeds"] = []

    # wallets 키가 없거나 dict가 아니면 초기화
    if "wallets" not in data or not isinstance(data["wallets"], dict):
        data["wallets"] = {}

    return data
```

- [ ] **Step 4: 어설션 통과 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_load_data.py
```
Expected: `verify_load_data: all assertions PASS`

- [ ] **Step 5: RSS 동작 회귀 검증 — atomic save round-trip**

```powershell
& "venv\Scripts\python.exe" -c "import bot, json, os, tempfile; bot.DATA_FILE = os.path.join(tempfile.gettempdir(), '_rt.json'); bot.save_data({'feeds':[{'url':'x','channel_id':1}],'wallets':{'42':{'balance':100}}}); d=bot.load_data(); assert d['feeds'][0]['url']=='x'; assert d['wallets']['42']['balance']==100; os.remove(bot.DATA_FILE); print('roundtrip OK')"
```
Expected: `roundtrip OK`

- [ ] **Step 6: Commit**

```powershell
git add bot.py scripts\verify_load_data.py
git commit -m "feat(wallet): load_data에 wallets={} 기본값 추가 + 어설션 스크립트"
```

---

## Task 4: 권한·컨텍스트 가드 함수

**Goal:** `is_admin`, `_require_text_channel`, `_get_existing_guild_wallet` 가드 함수. `/모임통장 등록`, `/입금`, `/출금`, `/관리`의 공통 진입 검증.

**Files:**
- Modify: `wallet_cog.py` (가드 함수 추가)

- [ ] **Step 1: 어설션 스크립트에 가드 함수 호출 검증 추가**

Append to `D:\dev\eu-ssya-bot\scripts\verify_wallet.py`:

```python
# ---- 가드 함수 존재 확인 ----
import inspect
assert callable(W.is_admin), "is_admin not defined"
assert callable(W._require_text_channel), "_require_text_channel not defined"
assert callable(W._get_existing_guild_wallet), "_get_existing_guild_wallet not defined"

# _get_existing_guild_wallet 순수 로직
sample_wallets = {
    "100": {"guild_id": "G1", "balance": 50},
    "200": {"guild_id": "G2", "balance": 80},
}
hit = W._get_existing_guild_wallet(sample_wallets, "G1")
assert hit is not None and hit[0] == "100"
miss = W._get_existing_guild_wallet(sample_wallets, "G999")
assert miss is None

print("guard helpers OK")
```

- [ ] **Step 2: 어설션 실패 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `AssertionError: is_admin not defined` 또는 `AttributeError`.

- [ ] **Step 3: wallet_cog.py에 가드 함수 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **before** the `class WalletCog` line:

```python
# ---------------- Discord 의존 가드 함수 ----------------
def is_admin(interaction: discord.Interaction) -> bool:
    """interaction을 호출한 사용자가 Discord Administrator 권한이 있는지."""
    if interaction.guild is None or interaction.user is None:
        return False
    # GuildMember인 경우 guild_permissions 사용 가능
    perms = getattr(interaction.user, "guild_permissions", None)
    if perms is None:
        return False
    return perms.administrator


def _require_text_channel(interaction: discord.Interaction) -> Optional[str]:
    """텍스트 채널 + guild 가드. 통과하면 None, 실패하면 한국어 에러 메시지."""
    if interaction.guild is None:
        return "이 명령은 서버 채널에서만 사용할 수 있습니다."
    if not isinstance(interaction.channel, discord.TextChannel):
        return "일반 텍스트 채널에서만 사용할 수 있습니다."
    return None


def _get_existing_guild_wallet(
    wallets: Dict[str, dict], guild_id: str
) -> Optional[tuple]:
    """이 guild에 이미 등록된 wallet이 있으면 (channel_id_str, wallet_dict) 반환, 없으면 None."""
    for ch_id_str, wallet in wallets.items():
        if str(wallet.get("guild_id", "")) == str(guild_id):
            return (ch_id_str, wallet)
    return None
```

- [ ] **Step 4: 어설션 통과 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `verify_wallet: all assertions PASS` and `guard helpers OK`.

- [ ] **Step 5: py_compile + import 검증**

```powershell
& "venv\Scripts\python.exe" -m py_compile wallet_cog.py
& "venv\Scripts\python.exe" -c "import wallet_cog; print('OK')"
```
Expected: `OK`

- [ ] **Step 6: Commit**

```powershell
git add wallet_cog.py scripts\verify_wallet.py
git commit -m "feat(wallet): 권한/컨텍스트 가드 함수 (is_admin, _require_text_channel, _get_existing_guild_wallet)"
```

---

## Task 5: /모임통장 등록 명령

**Goal:** 채널을 모임통장으로 등록. 권한·컨텍스트·중복·서버 1개 제약 모두 가드.

**Files:**
- Modify: `wallet_cog.py` (mt_group + register 명령 + import 추가)

- [ ] **Step 1: 검증 시나리오 작성**

`/모임통장 등록`은 Discord interaction이 필요해서 어설션 스크립트로 자동 검증이 어렵다. **수동 시나리오**를 작성하여 Task 13의 최종 manual checklist에서 검증:

| # | 시나리오 | 기대 응답 |
|---|---------|----------|
| 1 | 일반 멤버가 `/모임통장 등록` | ephemeral: "이 명령은 서버 관리자만 사용할 수 있습니다." |
| 2 | DM 채널에서 `/모임통장 등록` | ephemeral: "이 명령은 서버 채널에서만 사용할 수 있습니다." |
| 3 | 관리자가 빈 channel에서 `/모임통장 등록` | non-ephemeral: "모임통장을 시작합니다. 초기 잔액: 0원..." |
| 4 | 같은 채널에 또 `/모임통장 등록` | ephemeral: "이 채널은 이미 모임통장으로 등록되어 있습니다." |
| 5 | 같은 서버 다른 채널에서 `/모임통장 등록` | ephemeral: "이 서버에는 이미 #기존채널 에 모임통장이 등록되어 있습니다..." |
| 6 | 봇에 Manage Channels 없는 채널에서 등록 | ephemeral: "봇에 '채널 관리' 권한이 필요합니다..." |

(자동화는 Discord API mock이 복잡해 v1 out of scope.)

- [ ] **Step 2: wallet_cog.py에 명령 + 그룹 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`.

**Add imports** at top (near existing imports):

```python
import asyncio
import uuid

from discord import app_commands

# bot.py의 공유 락/저장소
from bot import _data_lock, load_data, save_data
```

**Add helper before WalletCog class:**

```python
def _build_registered_response(initial_balance: int) -> str:
    """등록 성공 응답 문자열."""
    return (
        f"모임통장을 시작합니다. 초기 잔액: {format_krw(initial_balance)}\n"
        f"채널 이름이 곧 업데이트됩니다."
    )
```

**Add to `WalletCog` class body** (replace the existing empty body):

```python
class WalletCog(commands.Cog):
    """모임통장 명령 + UI + rename worker."""

    def __init__(self, bot: commands.Bot) -> None:
        self.bot = bot

    mt_group = app_commands.Group(
        name="모임통장",
        description="모임통장 (스터디 커뮤니티 운영비) 관리",
    )

    # ---------------- /모임통장 등록 ----------------
    @mt_group.command(name="등록", description="이 채널을 모임통장으로 등록합니다.")
    async def register(self, interaction: discord.Interaction) -> None:
        # 권한
        if not is_admin(interaction):
            await interaction.response.send_message(
                "이 명령은 서버 관리자만 사용할 수 있습니다.", ephemeral=True
            )
            return

        # 컨텍스트
        err = _require_text_channel(interaction)
        if err:
            await interaction.response.send_message(err, ephemeral=True)
            return

        channel = interaction.channel
        guild = interaction.guild
        me = guild.me
        if me is None or not channel.permissions_for(me).manage_channels:
            await interaction.response.send_message(
                "봇에 '채널 관리' 권한이 필요합니다. 서버 설정에서 권한을 부여해주세요.",
                ephemeral=True,
            )
            return

        ch_key = str(channel.id)
        guild_id_str = str(guild.id)

        async with _data_lock:
            data = load_data()
            wallets = data["wallets"]

            # 이 채널 중복 등록
            if ch_key in wallets:
                await interaction.response.send_message(
                    "이 채널은 이미 모임통장으로 등록되어 있습니다.", ephemeral=True
                )
                return

            # 같은 서버 다른 채널 등록 충돌
            existing = _get_existing_guild_wallet(wallets, guild_id_str)
            if existing is not None:
                existing_ch_id, _ = existing
                await interaction.response.send_message(
                    f"이 서버에는 이미 <#{existing_ch_id}> 에 모임통장이 등록되어 있습니다. "
                    f"한 서버에는 한 개만 가능합니다.",
                    ephemeral=True,
                )
                return

            # 등록
            wallets[ch_key] = {
                "guild_id": guild_id_str,
                "balance": 0,
                "original_name": channel.name,
                "created_at": _now_kst_iso(),
                "transactions": [],
            }
            save_data(data)
            _pending_balance[channel.id] = 0

        logger.info(
            "wallet register: channel=%s guild=%s by=%s",
            channel.id, guild.id, interaction.user.id,
        )
        await interaction.response.send_message(_build_registered_response(0))
```

- [ ] **Step 3: py_compile + import 스모크**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
Remove-Item Env:DISCORD_BOT_TOKEN -ErrorAction SilentlyContinue
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
```
Expected: `OK`

- [ ] **Step 4: register 함수 존재 확인**

```powershell
& "venv\Scripts\python.exe" -c "import wallet_cog; from wallet_cog import WalletCog; assert hasattr(WalletCog, 'mt_group'); print('mt_group OK')"
```
Expected: `mt_group OK`

- [ ] **Step 5: verify_wallet 통과 확인 (회귀 없음)**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 모든 assertion 통과

- [ ] **Step 6: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): /모임통장 등록 명령 (한 서버 1개 + 권한 + Manage Channels 가드)"
```

---

## Task 6: 자동 메시지 송신 helper + /모임통장 입금 명령

**Goal:** 거래 등록 시 채널에 한 줄 메시지 자동 송신 + JSON 트랜잭션 append + 채널명 갱신 큐잉.

**Files:**
- Modify: `wallet_cog.py` (송신 helper + 입금 명령)

- [ ] **Step 1: 메시지 포맷 helper 작성 (pure function)**

Add to `wallet_cog.py` **before** WalletCog class (near other utils):

```python
def _format_transaction_message(
    kind: str, amount: int, memo: str, date_str: str, new_balance: int
) -> str:
    """채널에 송신할 거래 한 줄 메시지.

    예: '📥 +50,000원 · 지각벌금 홍길동 · 2026-11-28 · 잔액: 285,000원'
    메모가 빈 문자열이면 메모 부분 생략.
    """
    if kind == "income":
        emoji = "📥"
        sign = "+"
        signed_amount = amount
    elif kind == "expense":
        emoji = "📤"
        sign = "-"
        signed_amount = -amount
    else:
        raise ValueError(f"unknown kind: {kind!r}")

    parts = [f"{emoji} {sign}{abs(signed_amount):,}원"]
    if memo:
        parts.append(memo)
    parts.append(date_str)
    parts.append(f"잔액: {new_balance:,}원")
    return " · ".join(parts)
```

- [ ] **Step 2: 메시지 포맷 어설션 추가**

Append to `D:\dev\eu-ssya-bot\scripts\verify_wallet.py`:

```python
# ---- _format_transaction_message ----
msg = W._format_transaction_message("income", 50000, "지각벌금 홍길동", "2026-11-28", 285000)
assert msg == "📥 +50,000원 · 지각벌금 홍길동 · 2026-11-28 · 잔액: 285,000원", msg

msg2 = W._format_transaction_message("expense", 15000, "", "2026-11-26", 270000)
assert msg2 == "📤 -15,000원 · 2026-11-26 · 잔액: 270,000원", msg2  # 메모 생략

msg3 = W._format_transaction_message("income", 100, "테스트", "2026-01-01", 100)
assert msg3 == "📥 +100원 · 테스트 · 2026-01-01 · 잔액: 100원", msg3

print("format_transaction_message OK")
```

- [ ] **Step 3: 어설션 통과 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `format_transaction_message OK`

- [ ] **Step 4: 거래 등록 공통 helper + /모임통장 입금 명령 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **inside `WalletCog` class** (after `register`):

```python
    async def _record_transaction(
        self,
        interaction: discord.Interaction,
        kind: str,
        amount: int,
        memo: str,
        date_str: Optional[str],
    ) -> None:
        """입금/출금 공통 로직. kind는 'income' 또는 'expense'."""
        # 권한
        if not is_admin(interaction):
            await interaction.response.send_message(
                "이 명령은 서버 관리자만 사용할 수 있습니다.", ephemeral=True
            )
            return

        # 컨텍스트
        err = _require_text_channel(interaction)
        if err:
            await interaction.response.send_message(err, ephemeral=True)
            return

        # 금액 / 메모 검증
        amt_err = validate_amount(amount)
        if amt_err:
            await interaction.response.send_message(amt_err, ephemeral=True)
            return
        memo = memo or ""
        memo_err = validate_memo(memo)
        if memo_err:
            await interaction.response.send_message(memo_err, ephemeral=True)
            return

        # 날짜 — 미입력 시 오늘
        if not date_str:
            date_str = _today_kst_iso()
        else:
            try:
                _parse_date(date_str)
            except ValueError as e:
                await interaction.response.send_message(
                    f"날짜는 YYYY-MM-DD 형식으로 입력해주세요. 예: 2026-11-28",
                    ephemeral=True,
                )
                return

        channel = interaction.channel
        ch_key = str(channel.id)

        # 트랜잭션 저장 + 잔액 갱신 (락 안)
        new_balance: int
        tx_id: str
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(ch_key)
            if wallet is None:
                await interaction.response.send_message(
                    "이 채널은 모임통장으로 등록되어 있지 않습니다. "
                    "/모임통장 등록 을 먼저 실행하세요.",
                    ephemeral=True,
                )
                return

            old_balance = int(wallet["balance"])
            new_balance = compute_new_balance(old_balance, kind, amount)

            # Overdraft 거부 (출금)
            if kind == "expense" and new_balance < 0:
                await interaction.response.send_message(
                    f"잔액({format_krw(old_balance)})보다 큰 출금은 등록할 수 없습니다.",
                    ephemeral=True,
                )
                return

            tx_id = str(uuid.uuid4())
            tx = {
                "id": tx_id,
                "kind": kind,
                "amount": int(amount),
                "memo": memo,
                "date": date_str,
                "ts": _now_kst_iso(),
                "user_id": str(interaction.user.id),
                "channel_message_id": None,  # send 후 갱신
            }
            wallet["transactions"].append(tx)
            wallet["balance"] = new_balance
            save_data(data)
            _pending_balance[channel.id] = new_balance

        # 채널 자동 메시지 — 락 밖에서 송신
        message_id: Optional[int] = None
        try:
            msg = await channel.send(
                _format_transaction_message(kind, amount, memo, date_str, new_balance)
            )
            message_id = msg.id
        except discord.HTTPException as e:
            logger.warning(
                "wallet auto-message send failed: channel=%s err=%s",
                channel.id, e,
            )

        # 메시지 ID 사후 저장 (락 다시)
        if message_id is not None:
            async with _data_lock:
                data = load_data()
                wallet = data["wallets"].get(ch_key)
                if wallet is not None:
                    for t in wallet["transactions"]:
                        if t["id"] == tx_id:
                            t["channel_message_id"] = str(message_id)
                            break
                    save_data(data)

        verb = "입금" if kind == "income" else "출금"
        sign = "+" if kind == "income" else "-"
        logger.info(
            "wallet %s: channel=%s amount=%d new_balance=%d user=%s",
            kind, channel.id, amount, new_balance, interaction.user.id,
        )
        await interaction.response.send_message(
            f"{verb} {sign}{amount:,}원 기록 완료. 현재 잔액: {format_krw(new_balance)}",
            ephemeral=True,
        )

    @mt_group.command(name="입금", description="입금을 기록합니다 (잔액 증가).")
    @app_commands.describe(
        금액="입금 금액 (정수, 원, 1 이상)",
        메모="거래 메모 (선택, 최대 200자)",
        날짜="YYYY-MM-DD 형식 (선택, 기본=오늘 KST)",
    )
    async def deposit(
        self,
        interaction: discord.Interaction,
        금액: int,
        메모: str = "",
        날짜: str = "",
    ) -> None:
        await self._record_transaction(interaction, "income", 금액, 메모, 날짜 or None)
```

- [ ] **Step 5: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 3개 모두 통과

- [ ] **Step 6: Commit**

```powershell
git add wallet_cog.py scripts\verify_wallet.py
git commit -m "feat(wallet): _format_transaction_message + _record_transaction 공통 helper + /모임통장 입금"
```

---

## Task 7: /모임통장 출금 명령 (overdraft 거부)

**Goal:** /모임통장 입금과 같은 공통 helper 재사용, kind="expense"로 호출.

**Files:**
- Modify: `wallet_cog.py` (WalletCog에 withdraw 메서드 추가)

- [ ] **Step 1: 검증 시나리오 (수동, Task 13에서)**

| # | 시나리오 | 기대 |
|---|---------|------|
| 1 | 일반 멤버가 `/모임통장 출금 금액:1000` | ephemeral: "관리자만..." |
| 2 | 미등록 채널에서 `/모임통장 출금 금액:1000` | ephemeral: "이 채널은 모임통장으로..." |
| 3 | 잔액 50000인데 `/모임통장 출금 금액:300000` | ephemeral: "잔액(50,000원)보다 큰 출금은..." |
| 4 | 잔액 50000인데 `/모임통장 출금 금액:50000` | 성공, 잔액 0원, 채널 메시지 `📤 -50,000원...` |
| 5 | `/모임통장 출금 금액:1000 날짜:2026-13-32` | ephemeral: "날짜는 YYYY-MM-DD..." |

- [ ] **Step 2: WalletCog에 withdraw 명령 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **inside `WalletCog` class** (after `deposit`):

```python
    @mt_group.command(name="출금", description="출금을 기록합니다 (잔액 감소).")
    @app_commands.describe(
        금액="출금 금액 (정수, 원, 1 이상, 잔액 이하)",
        메모="거래 메모 (선택, 최대 200자)",
        날짜="YYYY-MM-DD 형식 (선택, 기본=오늘 KST)",
    )
    async def withdraw(
        self,
        interaction: discord.Interaction,
        금액: int,
        메모: str = "",
        날짜: str = "",
    ) -> None:
        await self._record_transaction(interaction, "expense", 금액, 메모, 날짜 or None)
```

- [ ] **Step 3: py_compile + import 스모크**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 4: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): /모임통장 출금 명령 (overdraft 거부)"
```

---

## Task 8: 채널명 rename 워커 (디바운스 + 코얼레싱 + 429 처리)

**Goal:** `_pending_balance`에 쌓인 잔액을 5분 cooldown 지키며 채널명에 반영. 429 시 `retry_after`만큼 cooldown 연장.

**Files:**
- Modify: `wallet_cog.py` (rename_worker_loop + cog_load/unload)

- [ ] **Step 1: 워커 로직 어설션 (cooldown 계산 검증)**

Append to `D:\dev\eu-ssya-bot\scripts\verify_wallet.py`:

```python
# ---- cooldown 계산 검증 ----
# 429 시 _last_rename 값: now + bump - COOLDOWN
# 다음 체크: cur_now - _last_rename < COOLDOWN ? skip : proceed
# 의도: bump 만큼 기다리도록

COOLDOWN = W.RENAME_COOLDOWN_SECONDS
t0 = 1000.0
# retry_after=600 인 경우 bump = max(COOLDOWN, 600) = 600
bump = max(COOLDOWN, 600.0)
last_rename = t0 + bump - COOLDOWN  # = 1000 + 600 - 300 = 1300
# 다음 워커 틱 t=1060: 1060 - 1300 = -240 < 300 → skip ✓
# 다음 워커 틱 t=1600: 1600 - 1300 = 300 ≥ 300 → proceed ✓
assert (1060 - last_rename) < COOLDOWN  # skip
assert (1600 - last_rename) >= COOLDOWN  # proceed

# retry_after 없는 일반 성공: _last_rename = now, 다음 시도는 now+COOLDOWN 후
t0 = 2000.0
last_rename_ok = t0
assert (t0 + COOLDOWN - 1 - last_rename_ok) < COOLDOWN
assert (t0 + COOLDOWN - last_rename_ok) >= COOLDOWN

print("cooldown math OK")
```

- [ ] **Step 2: 어설션 통과 확인 (워커 미구현이라도 수학만 검증)**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: `cooldown math OK`

- [ ] **Step 3: WalletCog에 워커 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. **Add import** at top:

```python
from discord.ext import commands, tasks
```

(`tasks`를 import 추가; `commands`는 이미 있음)

**Add inside `WalletCog` class** (after `withdraw`, before any closing):

```python
    # ---------------- 채널명 rename 워커 ----------------
    @tasks.loop(seconds=RENAME_WORKER_INTERVAL_SECONDS)
    async def rename_worker_loop(self) -> None:
        if not _pending_balance:
            return

        loop = asyncio.get_running_loop()
        now = loop.time()

        # 락 안에서 메타데이터 스냅샷만 — 네트워크 호출 없음
        to_process: list = []
        async with _data_lock:
            data = load_data()
            wallets = data.get("wallets", {})
            for ch_id, desired_balance in list(_pending_balance.items()):
                last = _last_rename.get(ch_id, 0.0)
                if now - last < RENAME_COOLDOWN_SECONDS:
                    continue
                wallet = wallets.get(str(ch_id))
                if wallet is None:
                    # 등록 해제됨 — 큐 정리
                    _pending_balance.pop(ch_id, None)
                    continue
                to_process.append((ch_id, int(desired_balance)))

        # 락 밖에서 실제 channel.edit (개별 try/except)
        for ch_id, desired_balance in to_process:
            channel = self.bot.get_channel(ch_id)
            if channel is None:
                logger.info("rename worker: channel %s not found", ch_id)
                _pending_balance.pop(ch_id, None)
                continue

            new_name = _format_channel_name(desired_balance)
            try:
                await channel.edit(name=new_name, reason="모임통장 잔액 업데이트")
            except discord.HTTPException as e:
                status = getattr(e, "status", None)
                retry_after = getattr(e, "retry_after", None)
                if status == 429:
                    logger.warning(
                        "rename rate-limited: channel=%s retry_after=%s",
                        ch_id, retry_after,
                    )
                    bump = max(RENAME_COOLDOWN_SECONDS, float(retry_after or 0))
                    _last_rename[ch_id] = (
                        asyncio.get_running_loop().time() + bump - RENAME_COOLDOWN_SECONDS
                    )
                    continue
                logger.warning(
                    "rename failed: channel=%s status=%s err=%s",
                    ch_id, status, e,
                )
                _pending_balance.pop(ch_id, None)
                continue
            except Exception as e:  # noqa: BLE001
                logger.warning("rename unexpected error: channel=%s err=%s", ch_id, e)
                _pending_balance.pop(ch_id, None)
                continue

            # 성공
            _last_rename[ch_id] = asyncio.get_running_loop().time()
            # 같은 desired_balance인 경우만 pop — 새 값이 들어왔으면 유지
            if _pending_balance.get(ch_id) == desired_balance:
                _pending_balance.pop(ch_id, None)
            logger.info(
                "rename committed: channel=%s new_name=%s",
                ch_id, new_name,
            )

    @rename_worker_loop.before_loop
    async def _before_rename_worker_loop(self) -> None:
        await self.bot.wait_until_ready()
        logger.info("wallet rename worker loop ready")

    async def cog_load(self) -> None:
        self.rename_worker_loop.start()

    async def cog_unload(self) -> None:
        self.rename_worker_loop.cancel()
```

- [ ] **Step 4: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 5: 락 discipline grep 검증 (이 task 시점에서 가장 중요)**

```powershell
# _data_lock 블록 안에서 await channel.edit 호출이 0건이어야
& "venv\Scripts\python.exe" -c "
import re
content = open('wallet_cog.py', encoding='utf-8').read()
# Find all 'async with _data_lock:' blocks
# This is a heuristic — manually review if grep finds anything
import sys
lines = content.split('\n')
in_lock = False
indent_lock = 0
bad = []
for i, line in enumerate(lines, 1):
    stripped = line.lstrip()
    indent = len(line) - len(stripped)
    if 'async with _data_lock' in stripped:
        in_lock = True
        indent_lock = indent
        continue
    if in_lock and stripped and indent <= indent_lock:
        in_lock = False
    if in_lock and 'channel.edit' in stripped:
        bad.append(f'Line {i}: {stripped[:80]}')
print('Bad lock-edit overlaps:', bad)
assert not bad, bad
print('Lock discipline OK (no channel.edit within _data_lock)')
"
```
Expected: `Lock discipline OK (no channel.edit within _data_lock)`

- [ ] **Step 6: Commit**

```powershell
git add wallet_cog.py scripts\verify_wallet.py
git commit -m "feat(wallet): rename 워커 (디바운스 + 5분 cooldown + 429 retry_after 처리)"
```

---

## Task 9: 관리 UI Step 1+2+3 — DateSelect, ItemSelect, DetailView

**Goal:** 임베드 + Select + Button으로 날짜→항목→디테일 3단계 흐름. 수정/삭제는 Task 10+11에서.

**Files:**
- Modify: `wallet_cog.py` (Views 클래스들 추가)

- [ ] **Step 1: 검증 시나리오 (수동, Task 13)**

| # | 시나리오 | 기대 |
|---|---------|------|
| 1 | 거래 없는 채널에서 `/모임통장 관리` | ephemeral: "등록된 거래가 없습니다." |
| 2 | 거래 있는 채널에서 `/모임통장 관리` | ephemeral: 날짜 셀렉트 표시 |
| 3 | 날짜 선택 | 임베드 갱신: 항목 셀렉트 + [↩️ 다른 날짜] 버튼 |
| 4 | 항목 선택 | 임베드: 디테일 + [✏️ 수정][🗑️ 삭제][↩️ 뒤로] |
| 5 | [↩️ 뒤로] | 항목 셀렉트로 복귀 |
| 6 | View 10분 timeout | 임베드 비활성화 |

- [ ] **Step 2: WalletCog 클래스 밖에 View 클래스들 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **after WalletCog class body** (모듈 레벨):

```python
# ---------------- 관리 UI Views ----------------
_KIND_LABEL = {"income": "📥 입금", "expense": "📤 출금"}


def _build_detail_embed(tx: dict, current_balance: int) -> discord.Embed:
    """단일 거래 디테일 임베드."""
    kind = tx.get("kind", "")
    label = _KIND_LABEL.get(kind, kind)
    sign = "+" if kind == "income" else "-"
    amount = int(tx.get("amount", 0))
    color = 0x57F287 if kind == "income" else 0xED4245

    embed = discord.Embed(
        title=f"{label} {sign}{amount:,}원",
        color=color,
    )
    embed.add_field(name="메모", value=tx.get("memo") or "(없음)", inline=False)
    embed.add_field(name="날짜", value=tx.get("date", "-"), inline=True)
    user_id = tx.get("user_id", "")
    embed.add_field(
        name="등록자",
        value=f"<@{user_id}>" if user_id else "-",
        inline=True,
    )
    embed.add_field(name="현재 잔액", value=format_krw(current_balance), inline=True)
    embed.set_footer(text=f"ID: {tx.get('id', '')[:8]}…")
    return embed


def _build_date_embed(dates: list) -> discord.Embed:
    """날짜 선택 임베드."""
    embed = discord.Embed(
        title="📅 어떤 날짜의 거래를 보시겠어요?",
        description="최근 거래가 있는 날짜를 선택해주세요.",
        color=0x5865F2,
    )
    return embed


def _build_item_embed(date_str: str, tx_count: int) -> discord.Embed:
    embed = discord.Embed(
        title=f"📅 {date_str} 거래 {tx_count}건",
        description="수정/삭제할 항목을 선택해주세요.",
        color=0x5865F2,
    )
    return embed


class DateSelect(discord.ui.Select):
    def __init__(self, channel_id: int, dates_with_counts: list):
        options = []
        for date_str, count in dates_with_counts[:25]:
            options.append(
                discord.SelectOption(
                    label=f"{date_str} ({count}건)",
                    value=date_str,
                )
            )
        super().__init__(
            placeholder="날짜 선택",
            min_values=1,
            max_values=1,
            options=options,
        )
        self.channel_id = channel_id

    async def callback(self, interaction: discord.Interaction) -> None:
        date_str = self.values[0]
        # 해당 날짜 거래 로드
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None:
            await interaction.response.edit_message(
                content="모임통장이 더 이상 등록되어 있지 않습니다.",
                embed=None, view=None,
            )
            return
        txs = [t for t in wallet["transactions"] if t.get("date") == date_str]
        if not txs:
            await interaction.response.edit_message(
                content="이 날의 거래가 없습니다 (다른 곳에서 삭제되었을 수 있습니다).",
                embed=None, view=None,
            )
            return

        view = ItemSelectView(self.channel_id, date_str, txs)
        embed = _build_item_embed(date_str, len(txs))
        await interaction.response.edit_message(content=None, embed=embed, view=view)


class ItemSelect(discord.ui.Select):
    def __init__(self, channel_id: int, date_str: str, txs: list):
        options = []
        for t in txs[:25]:
            kind = t.get("kind", "")
            sign = "+" if kind == "income" else "-"
            label = _KIND_LABEL.get(kind, kind)
            memo = t.get("memo") or ""
            preview = memo if len(memo) <= 40 else memo[:37] + "..."
            display = f"{label} {sign}{int(t['amount']):,}원"
            if preview:
                display += f" · {preview}"
            options.append(
                discord.SelectOption(
                    label=display[:100],  # Select label은 100자 제한
                    value=t["id"],
                )
            )
        super().__init__(
            placeholder="항목 선택",
            min_values=1,
            max_values=1,
            options=options,
        )
        self.channel_id = channel_id
        self.date_str = date_str

    async def callback(self, interaction: discord.Interaction) -> None:
        tx_id = self.values[0]
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None:
            await interaction.response.edit_message(
                content="모임통장이 더 이상 등록되어 있지 않습니다.",
                embed=None, view=None,
            )
            return
        tx = next((t for t in wallet["transactions"] if t["id"] == tx_id), None)
        if tx is None:
            await interaction.response.edit_message(
                content="이 거래가 더 이상 존재하지 않습니다.",
                embed=None, view=None,
            )
            return

        view = DetailView(self.channel_id, self.date_str, tx_id)
        embed = _build_detail_embed(tx, int(wallet["balance"]))
        await interaction.response.edit_message(content=None, embed=embed, view=view)


class ItemSelectView(discord.ui.View):
    def __init__(self, channel_id: int, date_str: str, txs: list):
        super().__init__(timeout=VIEW_TIMEOUT_SECONDS)
        self.channel_id = channel_id
        self.add_item(ItemSelect(channel_id, date_str, txs))

    @discord.ui.button(label="↩️ 다른 날짜", style=discord.ButtonStyle.secondary)
    async def back_to_dates(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None or not wallet["transactions"]:
            await interaction.response.edit_message(
                content="등록된 거래가 없습니다.",
                embed=None, view=None,
            )
            return

        # 날짜별 거래 수 집계 (최근순)
        from collections import Counter
        date_counts = Counter(t["date"] for t in wallet["transactions"])
        dates_sorted = sorted(date_counts.items(), key=lambda kv: kv[0], reverse=True)

        view = DateSelectView(self.channel_id, dates_sorted)
        embed = _build_date_embed(dates_sorted)
        await interaction.response.edit_message(content=None, embed=embed, view=view)

    async def on_timeout(self) -> None:
        for item in self.children:
            item.disabled = True


class DateSelectView(discord.ui.View):
    def __init__(self, channel_id: int, dates_with_counts: list):
        super().__init__(timeout=VIEW_TIMEOUT_SECONDS)
        self.channel_id = channel_id
        self.add_item(DateSelect(channel_id, dates_with_counts))

    async def on_timeout(self) -> None:
        for item in self.children:
            item.disabled = True


class DetailView(discord.ui.View):
    def __init__(self, channel_id: int, date_str: str, tx_id: str):
        super().__init__(timeout=VIEW_TIMEOUT_SECONDS)
        self.channel_id = channel_id
        self.date_str = date_str
        self.tx_id = tx_id

    @discord.ui.button(label="↩️ 뒤로", style=discord.ButtonStyle.secondary)
    async def back_to_items(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None:
            await interaction.response.edit_message(
                content="모임통장이 더 이상 등록되어 있지 않습니다.",
                embed=None, view=None,
            )
            return
        txs = [t for t in wallet["transactions"] if t.get("date") == self.date_str]
        if not txs:
            await interaction.response.edit_message(
                content="이 날의 거래가 모두 삭제되었습니다.",
                embed=None, view=None,
            )
            return
        view = ItemSelectView(self.channel_id, self.date_str, txs)
        embed = _build_item_embed(self.date_str, len(txs))
        await interaction.response.edit_message(content=None, embed=embed, view=view)

    # [✏️ 수정], [🗑️ 삭제]는 Task 10/11에서 추가

    async def on_timeout(self) -> None:
        for item in self.children:
            item.disabled = True
```

- [ ] **Step 3: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 4: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): 관리 UI Step 1+2+3 — DateSelect / ItemSelect / DetailView 임베드 + 셀렉트 + 뒤로 버튼"
```

---

## Task 10: EditModal + 수정 로직

**Goal:** [✏️ 수정] 버튼 → Modal 팝업 (금액/메모/날짜) → 제출 시 거래 mutate + 메시지 edit + 잔액 재계산.

**Files:**
- Modify: `wallet_cog.py` (EditModal 클래스 + DetailView에 수정 버튼)

- [ ] **Step 1: 검증 시나리오 (수동)**

| # | 시나리오 | 기대 |
|---|---------|------|
| 1 | 디테일 화면 [✏️ 수정] 클릭 | Modal 팝업 (금액/메모/날짜 prefilled) |
| 2 | 금액을 음수 또는 0으로 변경 | ephemeral 검증 오류 |
| 3 | 날짜를 `2026-13-32`로 변경 | ephemeral 검증 오류 |
| 4 | 금액 증가 → overdraft 발생 (kind=expense 변경 후 잔액 음수) | ephemeral 거부 |
| 5 | 정상 수정 (메모만 변경) | ephemeral "수정 완료", 채널 메시지 edit됨, 잔액 변동 없음 |
| 6 | 금액 변경 → 잔액 재계산 | 채널명 5분 내 갱신 |

- [ ] **Step 2: EditModal 클래스 + DetailView에 [✏️ 수정] 버튼 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **before `class DetailView`** (or after, but the class needs to exist before `DetailView.edit_button` references it):

```python
class EditModal(discord.ui.Modal, title="거래 수정"):
    def __init__(self, channel_id: int, tx_id: str, current_tx: dict):
        super().__init__(timeout=VIEW_TIMEOUT_SECONDS)
        self.channel_id = channel_id
        self.tx_id = tx_id

        self.amount_input = discord.ui.TextInput(
            label="금액 (원)",
            default=str(int(current_tx.get("amount", 0))),
            required=True,
            max_length=20,
        )
        self.memo_input = discord.ui.TextInput(
            label="메모 (선택, 최대 200자)",
            default=current_tx.get("memo", "") or "",
            required=False,
            max_length=MAX_MEMO_LEN,
            style=discord.TextStyle.short,
        )
        self.date_input = discord.ui.TextInput(
            label="날짜 (YYYY-MM-DD)",
            default=current_tx.get("date", _today_kst_iso()),
            required=True,
            max_length=10,
        )
        self.add_item(self.amount_input)
        self.add_item(self.memo_input)
        self.add_item(self.date_input)

    async def on_submit(self, interaction: discord.Interaction) -> None:
        # 입력 검증
        try:
            new_amount = int(self.amount_input.value.strip())
        except (ValueError, TypeError):
            await interaction.response.send_message(
                "금액은 정수여야 합니다.", ephemeral=True
            )
            return
        amt_err = validate_amount(new_amount)
        if amt_err:
            await interaction.response.send_message(amt_err, ephemeral=True)
            return

        new_memo = (self.memo_input.value or "").strip()
        memo_err = validate_memo(new_memo)
        if memo_err:
            await interaction.response.send_message(memo_err, ephemeral=True)
            return

        new_date_str = (self.date_input.value or "").strip()
        try:
            _parse_date(new_date_str)
        except ValueError:
            await interaction.response.send_message(
                "날짜는 YYYY-MM-DD 형식으로 입력해주세요. 예: 2026-11-28",
                ephemeral=True,
            )
            return

        # 거래 mutate + 잔액 재계산
        ch_key = str(self.channel_id)
        new_balance: int
        prev_tx: dict
        new_message_text: str
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(ch_key)
            if wallet is None:
                await interaction.response.send_message(
                    "모임통장이 더 이상 등록되어 있지 않습니다.", ephemeral=True
                )
                return
            target = next(
                (t for t in wallet["transactions"] if t["id"] == self.tx_id), None
            )
            if target is None:
                await interaction.response.send_message(
                    "이 거래가 더 이상 존재하지 않습니다.", ephemeral=True
                )
                return

            # 수정 후 전체 잔액 시뮬레이션
            prev_tx = dict(target)
            target["amount"] = new_amount
            target["memo"] = new_memo
            target["date"] = new_date_str
            # 전체 sum으로 잔액 재계산
            recomputed = 0
            for t in wallet["transactions"]:
                if t["kind"] == "income":
                    recomputed += int(t["amount"])
                else:
                    recomputed -= int(t["amount"])
            if recomputed < 0:
                # Overdraft 거부 — 원복
                target["amount"] = prev_tx["amount"]
                target["memo"] = prev_tx["memo"]
                target["date"] = prev_tx["date"]
                await interaction.response.send_message(
                    f"수정 시 잔액이 음수({format_krw(recomputed)})가 됩니다. 거부됩니다.",
                    ephemeral=True,
                )
                return

            wallet["balance"] = recomputed
            new_balance = recomputed
            save_data(data)
            _pending_balance[self.channel_id] = recomputed

            # 채널 메시지 edit용 새 텍스트
            new_message_text = _format_transaction_message(
                target["kind"], new_amount, new_memo, new_date_str, new_balance
            )
            channel_message_id = target.get("channel_message_id")

        # 락 밖에서 채널 메시지 edit (있으면)
        if channel_message_id:
            channel = interaction.client.get_channel(self.channel_id)
            if channel is not None:
                try:
                    msg = await channel.fetch_message(int(channel_message_id))
                    await msg.edit(content=new_message_text)
                except discord.HTTPException as e:
                    logger.warning(
                        "wallet edit: message edit failed channel=%s msg=%s err=%s",
                        self.channel_id, channel_message_id, e,
                    )

        logger.info(
            "wallet edit: channel=%s tx=%s new_amount=%d new_balance=%d user=%s",
            self.channel_id, self.tx_id, new_amount, new_balance, interaction.user.id,
        )
        await interaction.response.send_message(
            f"수정 완료. 현재 잔액: {format_krw(new_balance)}", ephemeral=True
        )
```

**Add inside `DetailView` class** (after the `back_to_items` button method):

```python
    @discord.ui.button(label="✏️ 수정", style=discord.ButtonStyle.primary)
    async def edit_tx(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None:
            await interaction.response.send_message(
                "모임통장이 더 이상 등록되어 있지 않습니다.", ephemeral=True
            )
            return
        tx = next((t for t in wallet["transactions"] if t["id"] == self.tx_id), None)
        if tx is None:
            await interaction.response.send_message(
                "이 거래가 더 이상 존재하지 않습니다.", ephemeral=True
            )
            return
        await interaction.response.send_modal(EditModal(self.channel_id, self.tx_id, tx))
```

- [ ] **Step 3: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 4: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): EditModal + DetailView 수정 버튼 (overdraft 거부 + 채널 메시지 edit)"
```

---

## Task 11: ConfirmDeleteView + 삭제 로직 (hard delete)

**Goal:** [🗑️ 삭제] 버튼 → 확인 ephemeral 임베드 → [예, 삭제합니다] → JSON 제거 + 채널 메시지 delete + 잔액 재계산.

**Files:**
- Modify: `wallet_cog.py` (ConfirmDeleteView 클래스 + DetailView에 삭제 버튼)

- [ ] **Step 1: 검증 시나리오 (수동)**

| # | 시나리오 | 기대 |
|---|---------|------|
| 1 | 디테일 [🗑️ 삭제] 클릭 | ephemeral 확인 임베드 + [예, 삭제] / [취소] |
| 2 | [취소] | ephemeral: "취소되었습니다." |
| 3 | [예, 삭제] | ephemeral: "삭제 완료. 현재 잔액: ..." + 채널 자동 메시지 사라짐 |
| 4 | 같은 거래에 다시 [🗑️ 삭제] 후 [예, 삭제] | ephemeral: "이미 삭제됨" (멱등) |

- [ ] **Step 2: ConfirmDeleteView + DetailView에 [🗑️ 삭제] 버튼 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **before `class DetailView`** (모듈 레벨):

```python
class ConfirmDeleteView(discord.ui.View):
    def __init__(self, channel_id: int, tx_id: str, tx_preview: str):
        super().__init__(timeout=VIEW_TIMEOUT_SECONDS)
        self.channel_id = channel_id
        self.tx_id = tx_id
        self.tx_preview = tx_preview

    @discord.ui.button(label="예, 삭제합니다", style=discord.ButtonStyle.danger)
    async def confirm_delete(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        ch_key = str(self.channel_id)
        channel_message_id: Optional[str] = None
        new_balance: Optional[int] = None
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(ch_key)
            if wallet is None:
                await interaction.response.send_message(
                    "모임통장이 더 이상 등록되어 있지 않습니다.", ephemeral=True
                )
                return
            target = next(
                (t for t in wallet["transactions"] if t["id"] == self.tx_id), None
            )
            if target is None:
                await interaction.response.send_message(
                    "이미 삭제된 거래입니다.", ephemeral=True
                )
                return

            channel_message_id = target.get("channel_message_id")
            wallet["transactions"].remove(target)

            # 전체 잔액 재계산
            recomputed = 0
            for t in wallet["transactions"]:
                if t["kind"] == "income":
                    recomputed += int(t["amount"])
                else:
                    recomputed -= int(t["amount"])
            wallet["balance"] = recomputed
            new_balance = recomputed
            save_data(data)
            _pending_balance[self.channel_id] = recomputed

        # 락 밖에서 채널 메시지 delete (있으면)
        if channel_message_id:
            channel = interaction.client.get_channel(self.channel_id)
            if channel is not None:
                try:
                    msg = await channel.fetch_message(int(channel_message_id))
                    await msg.delete()
                except discord.HTTPException as e:
                    logger.warning(
                        "wallet delete: channel message delete failed channel=%s msg=%s err=%s",
                        self.channel_id, channel_message_id, e,
                    )

        logger.info(
            "wallet delete: channel=%s tx=%s new_balance=%d user=%s",
            self.channel_id, self.tx_id, new_balance, interaction.user.id,
        )
        await interaction.response.send_message(
            f"삭제 완료. 현재 잔액: {format_krw(new_balance)}", ephemeral=True
        )

    @discord.ui.button(label="취소", style=discord.ButtonStyle.secondary)
    async def cancel(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        await interaction.response.send_message("취소되었습니다.", ephemeral=True)
```

**Add inside `DetailView` class** (after `edit_tx`):

```python
    @discord.ui.button(label="🗑️ 삭제", style=discord.ButtonStyle.danger)
    async def delete_tx(
        self, interaction: discord.Interaction, button: discord.ui.Button
    ) -> None:
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(self.channel_id))
        if wallet is None:
            await interaction.response.send_message(
                "모임통장이 더 이상 등록되어 있지 않습니다.", ephemeral=True
            )
            return
        tx = next((t for t in wallet["transactions"] if t["id"] == self.tx_id), None)
        if tx is None:
            await interaction.response.send_message(
                "이미 삭제된 거래입니다.", ephemeral=True
            )
            return

        kind_label = _KIND_LABEL.get(tx.get("kind", ""), "")
        sign = "+" if tx.get("kind") == "income" else "-"
        preview = (
            f"{kind_label} {sign}{int(tx['amount']):,}원 · "
            f"{tx.get('memo') or '(메모 없음)'} · {tx.get('date', '')}"
        )

        view = ConfirmDeleteView(self.channel_id, self.tx_id, preview)
        embed = discord.Embed(
            title="🗑️ 정말 삭제하시겠어요?",
            description=(
                f"{preview}\n\n"
                "이 거래는 완전히 삭제되며 잔액에서 차감됩니다. "
                "채널의 자동 메시지도 함께 삭제됩니다."
            ),
            color=0xED4245,
        )
        await interaction.response.send_message(embed=embed, view=view, ephemeral=True)
```

- [ ] **Step 3: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 4: 락 discipline grep 재검증**

같은 grep 스크립트를 다시 실행해 새로 추가된 EditModal/ConfirmDeleteView 안에 lock-edit overlap 없는지 확인:

```powershell
& "venv\Scripts\python.exe" -c "
content = open('wallet_cog.py', encoding='utf-8').read()
lines = content.split('\n')
in_lock = False
indent_lock = 0
bad = []
for i, line in enumerate(lines, 1):
    stripped = line.lstrip()
    indent = len(line) - len(stripped)
    if 'async with _data_lock' in stripped:
        in_lock = True
        indent_lock = indent
        continue
    if in_lock and stripped and indent <= indent_lock:
        in_lock = False
    if in_lock:
        for bad_pat in ['channel.edit(', 'channel.send(', 'message.edit(', 'message.delete(', 'msg.edit(', 'msg.delete(', 'fetch_message(']:
            if bad_pat in stripped:
                bad.append(f'Line {i} ({bad_pat}): {stripped[:80]}')
assert not bad, bad
print('Lock discipline OK')
"
```
Expected: `Lock discipline OK`

- [ ] **Step 5: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): ConfirmDeleteView + DetailView 삭제 버튼 (hard delete + 채널 메시지 delete + 잔액 재계산)"
```

---

## Task 12: /모임통장 관리 명령

**Goal:** `/모임통장 관리` 명령으로 Step 1 (DateSelect) 진입 시작.

**Files:**
- Modify: `wallet_cog.py` (WalletCog에 manage 명령 추가)

- [ ] **Step 1: 검증 시나리오 (수동)**

| # | 시나리오 | 기대 |
|---|---------|------|
| 1 | 일반 멤버 `/모임통장 관리` | ephemeral 권한 거부 |
| 2 | 미등록 채널 `/모임통장 관리` | ephemeral: "모임통장으로 등록되어 있지 않습니다..." |
| 3 | 거래 없는 등록 채널 `/모임통장 관리` | ephemeral: "등록된 거래가 없습니다." |
| 4 | 거래 있는 채널 `/모임통장 관리` | ephemeral 임베드 + DateSelect |

- [ ] **Step 2: WalletCog에 manage 명령 추가**

Edit `D:\dev\eu-ssya-bot\wallet_cog.py`. Add **inside `WalletCog` class** (after `withdraw`, before `rename_worker_loop`):

```python
    @mt_group.command(name="관리", description="모임통장 거래를 수정/삭제합니다.")
    async def manage(self, interaction: discord.Interaction) -> None:
        # 권한
        if not is_admin(interaction):
            await interaction.response.send_message(
                "이 명령은 서버 관리자만 사용할 수 있습니다.", ephemeral=True
            )
            return

        # 컨텍스트
        err = _require_text_channel(interaction)
        if err:
            await interaction.response.send_message(err, ephemeral=True)
            return

        channel_id = interaction.channel.id
        async with _data_lock:
            data = load_data()
            wallet = data["wallets"].get(str(channel_id))

        if wallet is None:
            await interaction.response.send_message(
                "이 채널은 모임통장으로 등록되어 있지 않습니다. "
                "/모임통장 등록 을 먼저 실행하세요.",
                ephemeral=True,
            )
            return
        if not wallet["transactions"]:
            await interaction.response.send_message(
                "등록된 거래가 없습니다.", ephemeral=True
            )
            return

        # 날짜별 거래 수 집계
        from collections import Counter
        date_counts = Counter(t["date"] for t in wallet["transactions"])
        dates_sorted = sorted(date_counts.items(), key=lambda kv: kv[0], reverse=True)

        view = DateSelectView(channel_id, dates_sorted)
        embed = _build_date_embed(dates_sorted)
        await interaction.response.send_message(embed=embed, view=view, ephemeral=True)
```

- [ ] **Step 3: py_compile + import + verify_wallet**

```powershell
& "venv\Scripts\python.exe" -m py_compile bot.py wallet_cog.py
& "venv\Scripts\python.exe" -c "import bot; import wallet_cog; print('OK')"
& "venv\Scripts\python.exe" scripts\verify_wallet.py
```
Expected: 통과

- [ ] **Step 4: 4개 명령 모두 존재 확인**

```powershell
& "venv\Scripts\python.exe" -c "
import wallet_cog
from wallet_cog import WalletCog
g = WalletCog.mt_group
names = sorted(c.name for c in g.commands)
print('subcommands:', names)
assert names == ['관리', '등록', '입금', '출금'], names
print('all 4 commands OK')
"
```
Expected: `subcommands: ['관리', '등록', '입금', '출금']` 및 `all 4 commands OK`

- [ ] **Step 5: Commit**

```powershell
git add wallet_cog.py
git commit -m "feat(wallet): /모임통장 관리 명령 — DateSelect View 시작점"
```

---

## Task 13: 최종 정적 검증 + 락 discipline 코드 리뷰

**Goal:** 모든 spec AC를 grep + 어설션으로 검증. 락 discipline grep 최종 통과.

**Files:**
- Create: `scripts/verify_final.py`

- [ ] **Step 1: 최종 검증 스크립트 작성**

Create `D:\dev\eu-ssya-bot\scripts\verify_final.py`:

```python
"""모임통장 최종 정적 검증 — 모든 AC를 grep + 어설션으로 확인."""
import re
import sys

sys.path.insert(0, ".")

content = open("wallet_cog.py", encoding="utf-8").read()
bot_content = open("bot.py", encoding="utf-8").read()


def find_lines(text, pattern):
    return [(i + 1, line) for i, line in enumerate(text.split("\n")) if re.search(pattern, line)]


# AC1: 4개 슬래시 명령 모두 존재
for cmd in ["등록", "입금", "출금", "관리"]:
    matches = find_lines(content, rf'@mt_group\.command\(name="{cmd}"')
    assert matches, f"명령 /모임통장 {cmd} 누락"
print("AC1 (4 commands): OK")

# AC2: 한 서버 1개 가드
assert "_get_existing_guild_wallet" in content
assert "한 서버에는 한 개만" in content
print("AC2 (one wallet per guild): OK")

# AC3: Administrator 권한 체크
assert "is_admin(" in content
assert "guild_permissions" in content or "administrator" in content
print("AC3 (admin check): OK")

# AC4: overdraft 거부
assert "잔액(" in content and "보다 큰 출금" in content
print("AC4 (overdraft reject): OK")

# AC5: hard delete (transactions.remove 사용)
assert "transactions\"].remove(" in content or "wallet[\"transactions\"].remove(" in content
print("AC5 (hard delete): OK")

# AC6: 채널명 디바운스 큐
assert "_pending_balance" in content
assert "RENAME_COOLDOWN_SECONDS = 300" in content or "RENAME_COOLDOWN_SECONDS=300" in content
assert "@tasks.loop" in content
print("AC6 (rename debounce): OK")

# AC7: 429 처리 + retry_after
assert "discord.HTTPException" in content
assert "retry_after" in content
print("AC7 (429 retry_after): OK")

# AC8: load_data wallets 초기화
assert "\"wallets\"" in bot_content
assert "wallets\"] = {}" in bot_content
print("AC8 (load_data wallets init): OK")

# AC9: setup_hook이 wallet_cog load
assert "load_extension(\"wallet_cog\")" in bot_content
print("AC9 (load_extension): OK")

# AC10: 자동 메시지 포맷에 잔액 포함
assert "잔액:" in content
print("AC10 (auto message format): OK")

# AC11: 락 discipline — _data_lock 안에서 channel.edit/send/fetch_message/message.edit/delete 0건
lines = content.split("\n")
in_lock = False
indent_lock = 0
bad = []
forbidden = [
    "channel.edit(", "channel.send(", "fetch_message(",
    ".edit(content=", ".delete()", "msg.delete()", "msg.edit("
]
for i, line in enumerate(lines, 1):
    stripped = line.lstrip()
    indent = len(line) - len(stripped)
    if "async with _data_lock" in stripped:
        in_lock = True
        indent_lock = indent
        continue
    if in_lock and stripped and indent <= indent_lock:
        in_lock = False
    if in_lock:
        for pat in forbidden:
            if pat in stripped:
                bad.append(f"L{i} ({pat}): {stripped[:80]}")
assert not bad, "lock discipline violations:\n" + "\n".join(bad)
print("AC11 (lock discipline): OK")

# AC12: View timeout 10분
assert "VIEW_TIMEOUT_SECONDS = 600" in content
print("AC12 (view timeout 10min): OK")

print("\n=== ALL ACs PASS ===")
```

- [ ] **Step 2: 검증 실행**

```powershell
& "venv\Scripts\python.exe" scripts\verify_final.py
```
Expected: 12개 AC 모두 OK + `=== ALL ACs PASS ===`

만약 fail 발생 시: 해당 task로 돌아가서 fix 후 다시 실행.

- [ ] **Step 3: 모든 어설션 스크립트 통합 실행**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
& "venv\Scripts\python.exe" scripts\verify_load_data.py
& "venv\Scripts\python.exe" scripts\verify_final.py
```
Expected: 3개 모두 PASS

- [ ] **Step 4: 의존성 및 환경 검증**

```powershell
# 새 deps 없는지
git diff main..HEAD -- requirements.txt
# Expected: 빈 output

# 단일 파일 추가만 (wallet_cog.py + scripts/) 
git diff --stat main..HEAD
```

- [ ] **Step 5: 회귀 테스트 — RSS 동작 무영향**

```powershell
& "venv\Scripts\python.exe" -c "
import bot
# RSS 함수들 모두 존재
assert hasattr(bot, 'rss_loop')
assert hasattr(bot, 'fetch_feed')
assert hasattr(bot, 'load_data')
assert hasattr(bot, 'save_data')
assert hasattr(bot, '_data_lock')
print('RSS regression OK')
"
```
Expected: `RSS regression OK`

- [ ] **Step 6: Commit**

```powershell
git add scripts\verify_final.py
git commit -m "test(wallet): 최종 AC 검증 스크립트 + 락 discipline grep"
```

---

## Task 14: Manual testing prep + 문서화 + 최종 cleanup

**Goal:** README에 모임통장 사용법 한 섹션 추가. manual 시나리오 체크리스트 문서로 저장. 브랜치 merge 전 최종 정리.

**Files:**
- Modify: `README.md`
- Create: `docs/superpowers/specs/2026-05-25-모임통장-manual-checklist.md`

- [ ] **Step 1: README에 모임통장 섹션 추가**

Edit `D:\dev\eu-ssya-bot\README.md`. Find the `## 💬 Bot Commands` section and **after** the existing RSS commands table, add:

```markdown
### 모임통장 (스터디 커뮤니티 운영비)

| Command | 설명 |
| ------- | ---- |
| `/모임통장 등록` | 이 채널을 모임통장으로 등록 (한 서버 1개만, 초기 잔액 0원) |
| `/모임통장 입금 금액:<int> [메모] [날짜]` | 입금 기록 (잔액 증가) |
| `/모임통장 출금 금액:<int> [메모] [날짜]` | 출금 기록 (잔액 감소, 잔액 초과 거부) |
| `/모임통장 관리` | 임베드 UI로 거래 수정/삭제 (날짜→항목→버튼) |

모든 명령은 **서버 관리자(Discord Administrator) 권한**만 실행할 수 있습니다.
모든 멤버는 채널의 자동 메시지로 거래 내역을 조회할 수 있습니다.
잔액은 채널 이름에 자동 반영됩니다 (예: `💰-285,000원`).
봇에 **Manage Channels** 권한이 필요합니다 (채널명 변경용).

날짜 형식: `YYYY-MM-DD` (미입력 시 오늘 KST). 미래 날짜도 허용됩니다.
```

- [ ] **Step 2: Manual 체크리스트 문서 작성**

Create `D:\dev\eu-ssya-bot\docs\superpowers\specs\2026-05-25-모임통장-manual-checklist.md`:

```markdown
# 모임통장 — Manual Test Checklist

라이브 Discord 서버에서 v1 배포 전 1회 실행. 토큰을 가진 사용자가 직접 검증.

## Setup

- [ ] 테스트용 Discord 서버에 봇 초대 (Manage Channels 권한 포함)
- [ ] 빈 텍스트 채널 1개 준비
- [ ] 일반 멤버 1명 + 관리자 1명 확보

## 시나리오 1: 등록

- [ ] 일반 멤버가 `/모임통장 등록` → ephemeral 거부 "관리자만..."
- [ ] DM 채널 `/모임통장 등록` → ephemeral 거부 "서버 채널에서만..."
- [ ] 관리자가 채널에서 `/모임통장 등록` → 성공, 5분 내 채널명 `💰-0원`
- [ ] 같은 채널에 또 `/모임통장 등록` → ephemeral 거부 "이미 등록..."
- [ ] 같은 서버 다른 채널에서 `/모임통장 등록` → ephemeral 거부 "이미 #이전채널..."

## 시나리오 2: 입금

- [ ] `/모임통장 입금 금액:50000 메모:"지각벌금" 날짜:2026-11-28` → 성공 ephemeral, 채널 메시지 `📥 +50,000원 · 지각벌금 · 2026-11-28 · 잔액: 50,000원`
- [ ] `/모임통장 입금 금액:30000` (메모/날짜 생략) → 성공, 채널 메시지에 오늘 날짜
- [ ] `/모임통장 입금 금액:0` → ephemeral 거부 "1원 이상..."
- [ ] `/모임통장 입금 금액:-100` → ephemeral 거부
- [ ] `/모임통장 입금 금액:1000 날짜:2026-13-32` → ephemeral 거부 "YYYY-MM-DD..."
- [ ] `/모임통장 입금 금액:1000 메모:"x"*201` → ephemeral 거부 "200자 이하..."

## 시나리오 3: 출금 (overdraft 거부)

- [ ] 잔액 80,000원 상태에서 `/모임통장 출금 금액:200000` → ephemeral 거부 "잔액(80,000원)보다 큰..."
- [ ] `/모임통장 출금 금액:30000 메모:"부스트"` → 성공, 잔액 50,000원

## 시나리오 4: 채널명 갱신 (rate-limit)

- [ ] 5건 입금을 연속 빠르게 (1분 내) → 채널명은 1회만 rename (코얼레싱)
- [ ] 10분 동안 거래 추가 등록 → 채널명 갱신 최대 2회 (Discord 한도)

## 시나리오 5: 관리 UI

- [ ] 일반 멤버 `/모임통장 관리` → 권한 거부
- [ ] 거래 없는 등록 채널 `/모임통장 관리` → ephemeral "등록된 거래가 없습니다."
- [ ] 거래 있는 채널 `/모임통장 관리` → 날짜 셀렉트 표시
- [ ] 날짜 선택 → 항목 셀렉트 + [↩️ 다른 날짜] 버튼
- [ ] [↩️ 다른 날짜] → 날짜 셀렉트 복귀
- [ ] 항목 선택 → 디테일 임베드 + [✏️ 수정][🗑️ 삭제][↩️ 뒤로]
- [ ] [↩️ 뒤로] → 항목 셀렉트 복귀

## 시나리오 6: 수정

- [ ] [✏️ 수정] → Modal 팝업 (금액·메모·날짜 prefilled)
- [ ] 금액을 0으로 → ephemeral 거부
- [ ] 날짜를 `2026-13-99`로 → ephemeral 거부
- [ ] 메모만 변경, 금액 유지 → ephemeral 성공, 채널 메시지 edit됨, 잔액 변동 없음
- [ ] 금액 증가 → 잔액 재계산 + 5분 내 채널명 갱신
- [ ] 금액을 너무 크게 변경 → 잔액 음수 → ephemeral overdraft 거부, 원복

## 시나리오 7: 삭제 (hard delete)

- [ ] [🗑️ 삭제] → ephemeral 확인 임베드 + [예, 삭제합니다][취소]
- [ ] [취소] → ephemeral "취소되었습니다."
- [ ] [예, 삭제합니다] → ephemeral "삭제 완료", 채널 자동 메시지 사라짐, 잔액 재계산, 채널명 갱신
- [ ] 같은 거래에 다시 [🗑️ 삭제] → ephemeral "이미 삭제됨"

## 시나리오 8: 회귀 — RSS 무영향

- [ ] `/ping` → "pong from eu-ssya-bot"
- [ ] `/rss add <URL>` → 정상 추가
- [ ] `/rss list` → 정상 조회
- [ ] `/rss remove <URL>` → 정상 삭제
- [ ] 봇 재시작 후 RSS 폴링 정상 동작

## 시나리오 9: 봇 권한 없음

- [ ] 봇에서 Manage Channels 권한 회수 후 `/모임통장 등록` 시도 → ephemeral "봇에 '채널 관리' 권한이..."

## 시나리오 10: View timeout

- [ ] `/모임통장 관리` 실행 후 10분 동안 클릭 안 함 → View 비활성화

## 합격 기준

위 모든 항목 통과 시 v1 배포 가능. 한 가지라도 fail 시 해당 task로 돌아가 디버그.
```

- [ ] **Step 3: 어설션 스크립트 최종 실행 확인**

```powershell
& "venv\Scripts\python.exe" scripts\verify_wallet.py
& "venv\Scripts\python.exe" scripts\verify_load_data.py
& "venv\Scripts\python.exe" scripts\verify_final.py
```
Expected: 3개 모두 PASS

- [ ] **Step 4: 모든 task의 commit log 확인**

```powershell
git log --oneline main..HEAD
```
Expected: 약 14개 commit (Task 1 ~ Task 14)

- [ ] **Step 5: Commit + 브랜치 push (선택)**

```powershell
git add README.md docs/superpowers/specs/2026-05-25-모임통장-manual-checklist.md
git commit -m "docs(wallet): README에 모임통장 명령어 섹션 + manual test checklist 추가"

# 선택: GitHub로 push
# git push -u origin feat/wallet
```

- [ ] **Step 6: Manual checklist 실행 (사람)**

토큰을 가진 사용자가 `docs/superpowers/specs/2026-05-25-모임통장-manual-checklist.md`의 모든 항목을 직접 검증.

모두 통과 시: `feat/wallet` 브랜치를 `main`으로 merge.

---

## Self-Review

이 plan을 spec과 대조하여 다음 항목 확인:

**Spec coverage (14개 ACs)**:
- [x] AC1 4개 슬래시 명령 → Task 5, 6, 7, 12
- [x] AC2 한 서버 1개 → Task 5 (`_get_existing_guild_wallet` 가드)
- [x] AC3 Administrator 권한 → Task 4 (`is_admin`)
- [x] AC4 overdraft 거부 → Task 6 (입금·출금 공통), Task 10 (수정)
- [x] AC5 hard delete → Task 11 (`transactions.remove`)
- [x] AC6 디바운스 큐 → Task 2 (상수), Task 8 (워커)
- [x] AC7 429 retry_after → Task 8
- [x] AC8 load_data wallets 초기화 → Task 3
- [x] AC9 load_extension → Task 1
- [x] AC10 자동 메시지 잔액 포함 → Task 6 (`_format_transaction_message`)
- [x] AC11 락 discipline → Task 8, 11, 13 (grep 검증)
- [x] AC12 View timeout 10분 → Task 2 상수
- [x] AC13 메시지 edit/delete null 처리 → Task 10, 11 (`if channel_message_id:`)
- [x] AC14 user_id 수정 시 유지 → Task 10 (Modal은 금액·메모·날짜만 수정)

**Placeholder scan**: 모든 step에 실제 코드/명령 포함. "TODO" 없음.

**Type consistency**:
- `_pending_balance: Dict[int, int]` — 정수 channel_id 키 유지 (`_pending_balance[channel.id]`)
- `wallets[ch_key]` — 문자열 키 (`str(channel.id)`) 일관
- `transaction["id"]` — UUID str 일관
- `kind` — `"income"` / `"expense"` 문자열만 일관

**Out of scope 명시 (spec과 일치)**:
- `/모임통장 balance`, `/list`, `/stats`, `/reset` — 없음 ✓
- 다중 통화, 카테고리, 통계, 차트 — 없음 ✓
- pytest 풀스위트 — 어설션 스크립트로 대체 ✓
- 다중 가계부 (한 서버 내) — `_get_existing_guild_wallet` 가드로 차단 ✓

---

## 끝

Plan이 완료되면 사용자에게 다음 두 가지 실행 옵션을 제시:

**1. Subagent-Driven (추천)** — 매 task 새 서브에이전트 + task 사이 리뷰, 빠른 반복
**2. Inline Execution** — 현 세션에서 execution-plans skill로 일괄 + 체크포인트

어떤 방식으로 진행할지 사용자 선택.
